[project]
name = "stock-exchange-analyzer-web-scraping-python"
version = "0.1.0"
description = "Stock Exchange Analyzer Web Scraping Python"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "selenium>=4.39.0",
    "webdriver-manager>=4.0.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.14.10",
]
